# Anchor scripts
ANCHOR_PROVIDER_URL=https://api.devnet.solana.com

# Production devnet program. Contract scripts use PROGRAM_ID when deriving PDAs.
PROGRAM_ID=ETEEEssGKAAQEGwz3ggDcy9vzPAPtBjtb2KocdyLBMjh

# Staging devnet program (deployed via ./deploy-staging.sh).
# Program keypair: contract/keys/staging-keypair.json (gitignored)
# Staging helper: ./run-staging.sh demo:3-master-setup
# Generic npm helper: npm run demo:stage -- demo:3-master-setup
STAGING_PROGRAM_ID=3oxBawS9rzZxRhKzkCqeoy1tDpH4mhTHBrt8aUG6Mfot

# Cloudflare Worker HTTPS proxy for Switchboard feed (oracle-proxy/).
# Switchboard oracle nodes enforce HTTPS-only; AviationStack free plan is HTTP-only.
# Deploy the Worker first, then set PROXY_URL here.
# API key is stored as a Worker secret — NOT embedded here.
# See: oracle-proxy/README.md
PROXY_URL=https://riskmesh-aviation-proxy.<your-account>.workers.dev
