export declare enum TimeInterval {
    FIVE_MINUTES = 0,
    TEN_MINUTES = 1,
    FIFTEEN_MINUTES = 2,
    THIRTY_MINUTES = 3,
    ONE_HOUR = 4,
    TWO_HOURS = 5,
    SIX_HOURS = 6,
    TWELVE_HOURS = 7
}
export declare namespace TimeInterval {
    function toString(interval?: TimeInterval): string;
    function fromString(interval: string): TimeInterval;
}
//# sourceMappingURL=timeInterval.d.ts.map